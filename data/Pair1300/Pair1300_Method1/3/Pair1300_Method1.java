import java.util.*;

public class Target {

int long2int(long l){
  int m;
  if (l < (m=Integer.MAX_VALUE) && l > (m=Integer.MIN_VALUE))   return (int)l;
  return m;
}
  int __target__(long l){
  int m;
  if (l <= (m = Integer.MAX_VALUE) && l > (m=Integer.MIN_VALUE))   return (int)l;
  return m;
}

}